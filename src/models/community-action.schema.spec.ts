import { CommunityAction.Schema } from './community-action.schema';

describe('CommunityAction.Schema', () => {
  it('should be defined', () => {
    expect(new CommunityAction.Schema()).toBeDefined();
  });
});
